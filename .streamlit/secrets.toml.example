# Local development only. Copy this file to .streamlit/secrets.toml and paste your
# key. On Streamlit Community Cloud, set this in the app's Settings -> Secrets instead
# (do NOT commit the real secrets.toml — it is gitignored).
ANTHROPIC_API_KEY = "sk-ant-..."
